#include <vector>
using namespace std;

vector<int> sortedSquaredArray(vector<int> array) {
  // Write your code here.
	vector<int> ans;
	for(auto i : array) ans.push_back(i*i);
	sort(ans.begin(), ans.end());
  return ans;
}
