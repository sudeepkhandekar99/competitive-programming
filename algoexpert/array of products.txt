#include <vector>
//lmaoooo first attempt was so bad
using namespace std;

vector<int> arrayOfProducts(vector<int> array) {
  // Write your code here.
	int i, productX = 1,product = 1, size = array.size(), check = 0;
	for(i = 0; i < size; i++){
		if(array[i] != 0)	product *= array[i];
		else check = 1;
		productX *= array[i];		
	}
	for(i = 0; i < size; i++){
		if(check == 0) array[i] = product/array[i];
		else{
			if(array[i]!=0)	array[i]=0;
			else	array[i] = productX;
		}
	}
  return array;
}
