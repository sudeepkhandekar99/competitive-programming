#include <vector>
using namespace std;

int nonConstructibleChange(vector<int> coins) {
  // Write your code here.
	sort(coins.begin(),coins.end());
	int sum = 1;
	for(int i = 0; i < coins.size() and coins[i] <= sum; i++) sum+=coins[i];
  return sum;
}
